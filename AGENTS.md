# AGENTS.md

此文件为 Codex 在此仓库中工作时提供项目规范、内容格式和验证要求。

## 项目概述

清华大学电子系推免复习资料库。将各门课程的知识点整理为 **Obsidian Markdown 文件组**，便于在 Obsidian 中浏览和复习。

## 目录结构

```
<学科分类>/                    # 如数学、物理、信号、电路、计算
└── <XX> - <课程名>/           # 每门课程一个目录，XX 为课程代码缩写
    ├── raw/                   # 原始教材 PDF、教师讲义、试题等外部参考资料
    ├── res/                   # 图片、绘图等附件
    ├── README.md              # 课程首页：知识主线、重点结构与复习指引
    ├── QUESTIONS.md           # 面试回忆题，作为 Longform scenes 的首项
    ├── <章节名>.md            # 各章节知识点
    ├─! CONTENT.md             # 应删除，章节顺序已由 frontmatter 中的 scenes 定义
    └─* EXPORT.md              # 可选的导出文件
```

`raw/` 存放该课程的原始资料。需要读取 PDF 时使用 Codex 的 `pdf` skill；涉及版面、公式或图形时应渲染相关页面进行核对，不能只依赖文本提取。

目前已完成 Markdown 化的参考范本：**SS - 信号与系统**、**EM - 电磁场与波**、**SP - 固体物理基础**。

## Markdown 文件格式规范

### 文件组织

+ 每章一个 `.md` 文件，文件名 = 章节标题（如 `Fourier 变换 (FT).md`）
+ 文件边界以**知识职责**而非教材顺序、课件课次或原始 Markdown 文件为准。重构时必须先建立概念分类和依赖关系，再迁移内容；不得只把旧文件原样放进新父章节。
+ Longform `scenes` 的顶层应表达课程的并列主干，中间层收纳共享理论或稳定的问题族，最低层应是**最小完整复习单元**，而不是越细越好。一个对象的直接变体、典型实现、基本操作和强绑定算法应优先在同一文件中形成解释闭环，不得只因为它们能够分别命名就拆成多个 scene。
+ 定义、符号、共同前提、通用评价维度和跨多个子章节的方法应上提到最低的共同层级，不得因为它们在原始资料中首先出现于某个子主题，就把它们留在该子章节内。「上提」可以是移到同一文件的高层标题，不等于必须新建父文件。
+ `README.md` 为课程首页，包含：
  + YAML frontmatter（`longform` 配置，`QUESTIONS` 固定为 `scenes` 首项，其后列出正式章节）
  + 课程知识主线与章节依赖关系
  + 重点结构及面向推免面试的复习指引
  + 必要的知识地图或引导图片（可用 `![[]]` 嵌入图片）
+ 附件（图片、SVG）放在课程的 `res/` 子目录下；附件路径由 Obsidian 的仓库设置统一管理，不写入章节 frontmatter

### Frontmatter 格式（README.md）

```yaml
---
longform:
  format: scenes
  title: <课程名称>
  workflow: Default Workflow
  sceneFolder: /
  scenes:
    - QUESTIONS
    - <章节1>
    - <章节2>
    - <父章节>
    - - <子章节>       # 嵌套数组表示子章节比前一个父章节深一级
  ignoredFiles:
    - CONTENT
    - EXPORT
---
```

### 标题层级

一级标题保留给文件名，章节标题使用二级标题（`##`）开始，子章节依次使用 `###`、`####` 等。

标题前不添加编号，允许使用 LaTeX 宏（如 `\section{\(z\) 变换的定义}` 转为 `## $z$ 变换的定义`）。

### 数学公式

+ 行内公式：`$...$`，不论是否内嵌 `cases` 等环境都必须**保持在同一行内**，公式前后与文本之间至少一个空格，与标点符之间可不留空格
+ 块级公式：`$$...$$`，两个 `$$` 必须**单独成行**
+ 使用 Obsidian 兼容的 LaTeX 语法
+ 自定义宏：`\dif`（微分 d）、`\e`（自然常数 e）、`\I`（虚数 i）、`\v{...}`（向量）、`\dint`（积分）等

### Callout 语法（Obsidian 特有）

```markdown
> [!definition] 标题
> 内容

> [!theorem] 标题
> 内容

> [!note] 标题
> 内容

> [!tip]
> 内容

> [!danger] 标题
> 内容
```

### 内部链接与图片

+ Wikilink 链接：`[[文件名]]` 或 `[[文件名|显示文本]]`
+ 图片嵌入：`![[图片名.png]]`（图片在 `res/` 目录下）
+ 块引用：`[[文件名#^blockid]]`，标注块 ID `^blockid` 应放在引用内容所在行末并间隔一个空格，对 Callout 块放在**标题行**末

### 扩展表格（tx 代码块）

通过 [Table Extended](https://github.com/aidenlx/table-extended) 插件支持扩展的 MultiMarkdown 表格语法，用 ` ```tx ` 代码块包裹。支持以下特性：

**单元格合并**：
+ `^^` 占位符表示该单元格与**上方单元格**合并（纵向合并）。常用于首列的分类标签跨行
+ `||` 在行末表示该单元格**横向合并**至剩余所有列

**行续接**：
+ 行末 `\` 将下一行续接到当前行，使一个逻辑行可跨多个物理行，用于在单元格内嵌入多行内容（列表、代码块等）

**对齐**：
+ 分隔行中 `:---` 左对齐、`:---:` 居中、`---:` 右对齐

示例：

````markdown
```tx
| 信息类别 | 具体内容 | 说明 |
|:---------|:---------|:-----|
| **进程描述信息** | PID | 唯一整数 |
| ^^ | 进程名 | 通常基于可执行文件名 |
| ^^ | 用户标识符 (UID) | |
| **进程控制信息** | 当前状态 | |
| ^^ | 优先级 | |
```
````

其中 `^^` 表示与上一行的第一列合并，使「进程描述信息」跨越 3 行，「进程控制信息」跨越 2 行。

## 行文风格

本节描述知识点 Markdown 文件的行文惯例，参考范本为 **SS - 信号与系统**、**SP - 固体物理基础**（中文为主）和 **EM - 电磁场与波**（英文为主）。

### 段落与节奏

+ 段落简短（1–3 句），公式与文字交替，避免长段纯文字
+ 关键术语首次出现时**加粗**，紧接着给出简短定义
+ 概念引入遵循三步：**粗体术语** → 一句话解释 → 可选的形式化 callout

```
**线性性**是指系统的响应与激励成正比关系。若系统对激励 $e_1(t)$、$e_2(t)$ 的响应分别为 $r_1(t)$、$r_2(t)$，则对激励 $e(t) = a_1 e_1(t) + a_2 e_2(t)$ 的响应为 $r(t) = a_1 r_1(t) + a_2 r_2(t)$。

> [!definition] 线性时不变系统
> **线性时不变（LTI）系统**是同时满足**线性性**和**时不变性**的系统。
```

+ 常用逻辑连接词：`即`（转述公式）、`则`（推出结论）、`其中`（解释符号）、`因此`
+ 非必要不使用括号标注补充内容，而是写入完整句子。例如「程序推进的时序不可再现是允许的，这是并发本身的特征」优于「程序推进的时序不可再现是允许的（并发本身的特征）」
+ 术语附带英文原文时，将原文纳入加粗范围内，使用半角括号、英文小写（专有名词除外）：`**进程 (process)**` 而非 `**进程**（Process）`。缩写全大写保持，如 `**PCB (Process Control Block)**`

### Callout 使用约定

| Callout | 用途 | 示例场景 |
|---------|------|---------|
| `[!definition]` | 形式化定义 | 核心概念的精确定义 |
| `[!theorem]` | 定理、定律、性质 | 卷积定理、Gauss 定律 |
| `[!note]` | 补充说明、概念间关系 | "Laplace 变换与 Fourier 变换的关系" |
| `[!note]+` | 可展开的详细说明 | 较长证明过程 |
| `[!example]` | 典型例题 | 具体计算示例 |
| `[!example]+` | 可展开的长例题 | 需多步推导的示例 |
| `[!tip]` | 实用技巧、记忆口诀 | "微分运算在系统可逆性角度不可逆" |
| `[!danger]` | 常见误区、重要警告 | 易混淆的概念辨析 |

+ 每个章节的 callout 不宜过多（一般 4–8 个），作为聚焦的知识路标
+ `[!note]` 常用于比较和桥接不同概念（如不同变换之间的关系）
+ `[!example]+` 用于较长例题，标题格式 `XXX的应用：示例` 或 `主题—具体题目`
+ `[!note]-` 可折叠，用于非核心的补充推导

### 公式编排

+ 行内公式用于变量引用和简短符号，与周围中文之间留一空格
+ 公式前后常有一句简短引导："满足…"、"定义为…"、"即"、"其中"，尽量不使用冒号
+ 重要公式用 `\mark{...}` 包裹以突出标记
+ 多行推导使用 `\begin{align}`，可嵌套使用 `\begin{aligned}`，关键步用 `\implies` 连接
+ 对较长公式，可在公式后用 `+` 列表逐项解释符号（"where:" 风格）：

```
$$
\v{E}(\v{r};\v{r}') = \dfrac{q}{4\pi\varepsilon_{0}\mathscr{r}^2} \vu{\mathscr{r}}
$$
其中：
+ $q$ 为点电荷电量
+ $\v{r}$ 为场点位置
+ $\v{r}'$ 为源点位置
```

### Wikilink 使用

+ 章节间链接使用 `[[目标章节名]]`，跨课程链接使用完整路径
+ 块链接使用 `[[目标章节#^blockid|显示文本]]`，应始终提供显示文本
+ block ID `^blockid` 放在被引用行的**行末**（同一行，间隔一空格）
+ 对callout的块引用，block ID放在callout **标题行**末
+ 图片嵌入 `![[图片名.png]]`，图片在 `res/` 目录下

### 面试回忆题（QUESTIONS.md）

课程目录中的 `QUESTIONS.md` 用于按知识点整理面试回忆题，固定作为 Longform `scenes` 的首项，便于先按真题诊断、再进入正式章节复习。其内容仍不是普通知识章节，遵循以下专用约定：

+ 文件开头先说明该课程通常在哪个面试组考察，并概括主要考察内容、常见追问方向和题型特点。
+ 二级标题用于按知识领域进一步分类，不使用「原题与参考解答」等无信息量标题；三级标题使用 `Q01 题目主题` 格式并连续编号。
+ 每条原题使用列表项承载。出处放在题目最前，格式为 `([20XX](test/20XX-...))`；多个出处放在同一对括号内并用逗号分隔。题目正文不加引号，不标注 A、B、C 等可信度等级。
+ 必须回到来源文件核对题意。允许修正断句和明显的语病，但不得把复习提纲、推测的追问或参考答案改写成原题；同一知识点的多个独立问题可以并列，不同问题若需要彼此割裂的答案，应拆成不同题目。
+ 原题之后使用 `> [!note]- 分析与解答` 默认收纳的 Callout，内部依次写 `**分析**`、`**解答**`、`**拓展**` 和 `**关联**，不再设置「原题」「参考解答」小标题。此处是对「普通正文不全部装入 Callout」规则的特例。
+ 「分析」具体说明题目考察的知识、合理切入点、成立条件和容易忽略之处。不得写大模型思维过程、作答指令或可套用于任意题目的模板句；简单题从简，复杂题按需要展开。
+ 「解答」完整回答全部原题，明确采用的模型、物理量、方程名称、条件和结论，并与正式课程笔记的深度一致。不能只给一句结论，也不能用「应该说明」「需要提到」代替实际说明。
+ 「拓展」只补充原题没有明确问到、但与理解该主题直接相关的关键知识、极限情形、工程应用或易混概念，不再重复作答要求，也不以继续提问代替内容。
+ 「关联」只链接正式课程笔记，优先精确到 `[[完整路径/文件名#内部标题]]`；不链接 `QUESTIONS.md` 自身。若课程笔记尚未覆盖该主题，应如实注明，不用相邻但无直接关系的链接代替。

### 列表

+ 无序列表使用 `+`，用于枚举性质、分类、步骤
+ 有序列表用于严格的步骤序列（如求解步骤）
+ 子列表应当缩进4空格
+ 列表项中可嵌入公式，公式需额外缩进对齐

### 标点与语言

+ 句子必须带有完整标点，不论是否在列表等环境中，除非结尾「即」「有」「得」紧接行间公式等
+ 使用全角标点 `，` `。` `：` `；` `、`，但内部**只有英文**的括号使用半角括号 `()`，内部含中文时使用全角括号 `（）`
+ 引号仅可使用直角引号 `「」`，不可使用弯引号 `“”` 或直引号 `""`
+ 公式前后与中文之间至少一个空格，而英文与中文之间可不留空格（渲染结果会自动留空），但加粗块另论：
  + 正例：`$\mathcal{F}(\cdot)$ 称为 **Fourier变换**，由Fourier于1822年提出。`
  + 反例：`$\mathcal{F}(\cdot)$称为**Fourier 变换**，由 Fourier 于 1822年 提出。`（必要的空格被省略，导致渲染后词语粘连）
+ 避免口语化感叹词、主观评价、emoji

### 章节结构惯例

每个章节 `.md` 文件通常按以下逻辑组织：
1. 起源/定义/引入背景
2. 基本性质（按子节分列各性质）
3. 典型信号/典型应用
4. 与其他概念的关系（可选）
5. 附录/扩展（可选）

### 章节分层构建流程

对于有父子层级关系的章节，采用自上而下再自下而上的构建方式：

1. **先脱离原始顺序建立知识地图**：提取概念、问题、方法和共同前提，用统一的分类标准形成并列主干。对旧文件先建立「旧标题 → 新职责文件」迁移表，不得把原始出现位置当作知识归属。
2. **搭建父章节与完整复习单元**：父章节说明共同对象、通用定义、共享方法和子章节分类标准；最低层文件应让用户能在一处完成对一个核心对象或问题的定义、表示、直接方法、典型应用和权衡的复习。
3. **迁移并撰写子章节**：按新职责移动完整知识单元，把跨多个子章节的内容上提到共同父章节，把一个旧文件中的弱相关主题拆到不同叶节。不得为了保留旧文件而制造错误的父子关系。
4. **回修父章节与 README**：子章节完成后，根据实际职责回修概要、`scenes` 和 Wikilink，并确认同层子章节使用同一分类维度。

这一流程确保课程目录结构从始至终保持连贯，各层级之间的概要描述与实际内容一致。

## 知识内容提取与整理

从教材、讲义、PDF、LaTeX 或既有笔记中提取知识，新建或重构课程章节，判断章节拆分、合并与父子层级，或审核知识覆盖和组织质量时，使用项目级 `organize-course-knowledge` skill。该 skill 负责从来源盘点、知识地图、文件层级、正文撰写到回修和验证的完整流程；不得仅按教材目录、课件课次或源文件 section 机械分节。

## LaTeX → Markdown 转换

转换流程与排版规则见项目级 `latex-to-markdown` skill，宏处理速查表见 `latex-macros` skill。处理 `.tex` 转换任务时应同时使用这两个 skill；若目标是正式课程知识笔记，而非仅做语法转换，还应同时使用 `organize-course-knowledge`，先确定知识结构和内容取舍，再执行 LaTeX 语法与资源转换。

## Codex 工作约定

+ `.agents/skills/` 存放本仓库的项目级 skill。修改 skill 时保持 `SKILL.md` 的 YAML frontmatter 有效，并同步修正其中引用的相对路径。
+ 不根据本文件中的静态表格推断课程进度。开始任务前检查目标课程的 `README.md`、`QUESTIONS.md`、章节文件和 `raw/` 资料，以仓库现状为准。
+ 不直接修改 `.obsidian/` 下的配置、插件数据、主题或缓存。涉及 Obsidian 使用方式时只检查现状并给出建议，除非用户明确要求修改该目录。
+ 不把 `raw/` 中的外部资料、导出文件或工具运行时状态当作正文整理结果提交。

## Obsidian 使用约定

+ 附件目录：`res/`（在 `.obsidian/app.json` 的 `attachmentFolderPath` 中配置）
+ 需要安装的社区插件：Longform（用于长篇写作场景管理）
+ 需要开启：Settings > Files & Links > Automatically update internal links
+ 忽略文件：`EXPORT.*`、`scripts/`

## 工具提示

+ LaTeX 公式在 Markdown 中的正确性需结合 Obsidian 渲染结果验证，不能仅靠源码检查。
+ 各课程的原始 PDF 教材、讲义等存放在 `raw/` 子目录下，使用 `pdf` skill 读取和渲染。
+ `.tex` 文件中的 tikz 绘图可转换为 Markdown `tikz` 代码块。
+ 编辑 `.md` 文件时，避免使用 Obsidian 不支持的 LaTeX 包，如 `nicematrix`、`tcolorbox`。

## 收录说明

本组覆盖编译与链接、声明与定义、基本类型、存储期、构建模式、机器字长和字符编码。

## 原题与参考解答

### Q01 从源代码到可执行文件

**原题**

+ 「C 程序从源文件到可执行文件经历哪些步骤？」 ([2024](test/2024-一字班推研面经-yj自行整理.zip)) 〔A/B 级〕
+ 「编译和链接有什么区别？头文件起什么作用？」 ([2024](test/2024-一字班推研面经-yj自行整理.zip)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C 程序从源文件到可执行文件经历哪些步骤？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「编译和链接有什么区别？头文件起什么作用？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

典型流程为预处理、编译、汇编、链接。预处理展开宏和头文件；编译完成语义检查并生成汇编或中间代码；汇编生成目标文件；链接解析跨目标文件的符号并完成重定位。头文件通常提供声明、类型和宏，不等同于库的实现。

**拓展**

+ 回答「从源代码到可执行文件」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q01 从源代码到可执行文件|从源代码到可执行文件]]。

### Q02 `#include <stdio.h>` 与函数声明

**原题**

+ 「不写 `#include <stdio.h>` 还能不能调用 `printf`？错误发生在编译还是链接？」 ([2009](test/2009-推研资料.docx)) ([2012](test/2012-面经_九字班2.pdf)) 〔A/B 级〕
+ 「声明和定义有什么区别？」 ([2009](test/2009-推研资料.docx)) ([2012](test/2012-面经_九字班2.pdf)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「不写 `#include <stdio.h>` 还能不能调用 `printf`？错误发生在编译还是链接？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「声明和定义有什么区别？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

`stdio.h` 提供 `printf` 的正确函数原型。C99 及以后调用未声明函数违反语言约束，编译器必须给出诊断，现代工具通常直接报错；早期 C 曾允许隐式 `int`，但可能因调用约定或参数类型不匹配产生未定义行为。若已有正确声明但最终没有链接对应实现，才是链接错误。声明告诉编译器名字与类型，定义还分配对象或提供函数体。

**拓展**

+ 回答「`#include <stdio.h>` 与函数声明」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q02 `#include <stdio.h>` 与函数声明|`#include <stdio.h>` 与函数声明]]。

### Q03 基本类型、整数溢出与浮点数

**原题**

+ 「C 语言有哪些基本数据类型和存储类型？」 ([2014](test/2014-一字班推研那点事儿（2）-宗泽宇.pdf)) 〔A/B 级〕
+ 「`int` 与 `float` 谁表示范围大、谁精度高？」 ([2014](test/2014-一字班推研那点事儿（2）-宗泽宇.pdf)) 〔A/B 级〕
+ 「整数溢出会怎样？浮点数为什么不能精确表示很多小数？」 ([2017](test/2017-面试经验-陈誉博.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 3 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C 语言有哪些基本数据类型和存储类型？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「`int` 与 `float` 谁表示范围大、谁精度高？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。
+ 对 「整数溢出会怎样？浮点数为什么不能精确表示很多小数？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。

**解答**

C 的基本算术类型包括整数类型、浮点类型和 `_Bool`，另有 `void`。同为常见 32 bit 表示时，`float` 的数量级范围远大于 `int`，但只有约 7 位十进制有效数字；`int` 在其有限范围内精确表示每个整数。无符号整数按模 $2^N$ 回绕，有符号整数溢出是未定义行为。二进制浮点只能精确表示分母为二的幂的有限小数，因此多数十进制小数需舍入。

**拓展**

+ 回答「基本类型、整数溢出与浮点数」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q03 基本类型、整数溢出与浮点数|基本类型、整数溢出与浮点数]]。

### Q04 `static` 与存储期

**原题**

+ 「`static` 有什么作用？」 ([2016](test/2016-推研面试经验分享_胡雨婷.pptx)) 〔A/B 级〕
+ 「局部静态变量、文件作用域静态变量和静态成员分别是什么？」 ([2018](test/2018_面试整体情况.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「`static` 有什么作用？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。
+ 对 「局部静态变量、文件作用域静态变量和静态成员分别是什么？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。

**解答**

函数内 `static` 对象具有静态存储期，只初始化一次但作用域仍局限于块；文件作用域的 `static` 函数或对象具有内部链接，只在当前翻译单元可见；C++ 静态数据成员属于类而非某个对象，静态成员函数没有 `this` 指针。

**拓展**

+ 回答「`static` 与存储期」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q04 `static` 与存储期|`static` 与存储期]]。

### Q15 Debug 与 Release

**原题**

+ 「Debug 与 Release 有什么区别？为什么程序可能只在一种模式出错？」 ([2012](test/2012-面经_九字班2.pdf)) 〔B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「Debug 与 Release 有什么区别？为什么程序可能只在一种模式出错？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

Debug 通常关闭或降低优化、保留符号和断言，便于单步调试；Release 开启优化并可能定义 `NDEBUG`。只在某模式失败常暴露未初始化变量、越界、数据竞争、悬空指针或对求值顺序的错误依赖，本质是程序含未定义行为，而非「优化器把正确程序优化错」。

**拓展**

+ 回答「Debug 与 Release」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q15 Debug 与 Release|Debug 与 Release]]。

### Q19 32 位与 64 位计算机

**原题**

+ 「32 位和 64 位计算机有什么区别？为什么现在更多使用 64 位？」 ([2024](test/2024-一字班推研面经_高希钢.docx)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「32 位和 64 位计算机有什么区别？为什么现在更多使用 64 位？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

位数通常指通用寄存器和虚拟地址等体系结构宽度，不表示所有指令都恰为该长度。64 位架构可提供更大的地址空间、更多或更宽寄存器及相应算术能力，但指针变大会增加内存占用和缓存压力。实际可寻址范围还受处理器实现和操作系统限制。

**拓展**

+ 回答「32 位与 64 位计算机」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q19 32 位与 64 位计算机|32 位与 64 位计算机]]。

### Q20 中文字符编码

**原题**

+ 「常见中文编码有哪些？怎样判断一段字节按某编码是否合法？」 ([2024](test/2024-一字班推研面经-李羿璇.pdf)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「常见中文编码有哪些？怎样判断一段字节按某编码是否合法？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。

**解答**

常见编码包括 GB2312、GBK、GB18030 和 Unicode 的 UTF-8、UTF-16 等编码形式。不能仅看某一字节就普遍判断「是不是中文」；需按选定编码的前导位、后续字节范围和码点映射验证完整字节序列。中国国家强制标准相关场景应关注 GB18030，不能把 GB2312笼统称为现行唯一官方中文编码。

**拓展**

+ 回答「中文字符编码」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS - 程序构建与数据表示#Q20 中文字符编码|中文字符编码]]。

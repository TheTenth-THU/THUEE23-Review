## 收录说明

计算机程序设计基础在所有有技术问题的年份中均出现。命题重点不是背语法，而是解释语言规则、内存与编译过程，并能把一段程序的行为说准确。

## 原题与参考解答

### Q01 从源代码到可执行文件

**原题**

+ 「C 程序从源文件到可执行文件经历哪些步骤？」 ([2024](test/2024-一字班推研面经-yj自行整理.zip)) 〔A/B 级〕
+ 「编译和链接有什么区别？头文件起什么作用？」 ([2024](test/2024-一字班推研面经-yj自行整理.zip)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C 程序从源文件到可执行文件经历哪些步骤？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「编译和链接有什么区别？头文件起什么作用？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

典型流程为预处理、编译、汇编、链接。预处理展开宏和头文件；编译完成语义检查并生成汇编或中间代码；汇编生成目标文件；链接解析跨目标文件的符号并完成重定位。头文件通常提供声明、类型和宏，不等同于库的实现。

**拓展**

+ 回答「从源代码到可执行文件」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q01 从源代码到可执行文件|从源代码到可执行文件]]。

### Q02 `#include <stdio.h>` 与函数声明

**原题**

+ 「不写 `#include <stdio.h>` 还能不能调用 `printf`？错误发生在编译还是链接？」 ([2009](test/2009-推研资料.docx)) ([2012](test/2012-面经_九字班2.pdf)) 〔A/B 级〕
+ 「声明和定义有什么区别？」 ([2009](test/2009-推研资料.docx)) ([2012](test/2012-面经_九字班2.pdf)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「不写 `#include <stdio.h>` 还能不能调用 `printf`？错误发生在编译还是链接？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「声明和定义有什么区别？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

`stdio.h` 提供 `printf` 的正确函数原型。C99 及以后调用未声明函数违反语言约束，编译器必须给出诊断，现代工具通常直接报错；早期 C 曾允许隐式 `int`，但可能因调用约定或参数类型不匹配产生未定义行为。若已有正确声明但最终没有链接对应实现，才是链接错误。声明告诉编译器名字与类型，定义还分配对象或提供函数体。

**拓展**

+ 回答「`#include <stdio.h>` 与函数声明」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q02 `#include <stdio.h>` 与函数声明|`#include <stdio.h>` 与函数声明]]。

### Q03 基本类型、整数溢出与浮点数

**原题**

+ 「C 语言有哪些基本数据类型和存储类型？」 ([2014](test/2014-一字班推研那点事儿（2）-宗泽宇.pdf)) 〔A/B 级〕
+ 「`int` 与 `float` 谁表示范围大、谁精度高？」 ([2014](test/2014-一字班推研那点事儿（2）-宗泽宇.pdf)) 〔A/B 级〕
+ 「整数溢出会怎样？浮点数为什么不能精确表示很多小数？」 ([2017](test/2017-面试经验-陈誉博.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 3 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C 语言有哪些基本数据类型和存储类型？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「`int` 与 `float` 谁表示范围大、谁精度高？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。
+ 对 「整数溢出会怎样？浮点数为什么不能精确表示很多小数？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。

**解答**

C 的基本算术类型包括整数类型、浮点类型和 `_Bool`，另有 `void`。同为常见 32 bit 表示时，`float` 的数量级范围远大于 `int`，但只有约 7 位十进制有效数字；`int` 在其有限范围内精确表示每个整数。无符号整数按模 $2^N$ 回绕，有符号整数溢出是未定义行为。二进制浮点只能精确表示分母为二的幂的有限小数，因此多数十进制小数需舍入。

**拓展**

+ 回答「基本类型、整数溢出与浮点数」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q03 基本类型、整数溢出与浮点数|基本类型、整数溢出与浮点数]]。

### Q04 `static` 与存储期

**原题**

+ 「`static` 有什么作用？」 ([2016](test/2016-推研面试经验分享_胡雨婷.pptx)) 〔A/B 级〕
+ 「局部静态变量、文件作用域静态变量和静态成员分别是什么？」 ([2018](test/2018_面试整体情况.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「`static` 有什么作用？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。
+ 对 「局部静态变量、文件作用域静态变量和静态成员分别是什么？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。

**解答**

函数内 `static` 对象具有静态存储期，只初始化一次但作用域仍局限于块；文件作用域的 `static` 函数或对象具有内部链接，只在当前翻译单元可见；C++ 静态数据成员属于类而非某个对象，静态成员函数没有 `this` 指针。

**拓展**

+ 回答「`static` 与存储期」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q04 `static` 与存储期|`static` 与存储期]]。

### Q05 指针、数组与二维数组

**原题**

+ 「二维数组元素怎样用指针表达？」 ([2012](test/2012-留下面经，造福众生-孟越.pdf)) ([2019](test/2019-推研面经-2.docx)) 〔A/B 级〕
+ 「数组名和指针是否完全相同？」 ([2019](test/2019-推研面经-2.docx)) 〔A/B 级〕
+ 「`int a[M][N]` 中 `a+1` 移动多少字节？」 ([2012](test/2012-留下面经，造福众生-孟越.pdf)) ([2014](test/2014-记一次人品大爆发-郭开元.pdf)) ([2019](test/2019-推研面经-1.docx)) ([2019](test/2019-推研面经-2.docx)) ([2019](test/2019-推研面经-3.docx)) ([2019](test/2019-推研面经-4.docx)) ([2019](test/2019-推研面经-5.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 3 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「二维数组元素怎样用指针表达？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。
+ 对 「数组名和指针是否完全相同？」，需要先给出带条件的结论，再用反例、极限情形或失败场景说明边界。
+ 对 「`int a[M][N]` 中 `a+1` 移动多少字节？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。

**解答**

在多数表达式中，数组名退化为指向首元素的指针，但数组本身不是可修改指针，`sizeof` 和取地址时也不退化。对 `int a[M][N]`，`a` 退化为 `int (*)[N]`，故 `a+1` 跨过一整行，即 $N\,\mathrm{sizeof(int)}$ 字节；`a[i][j]` 等价于 `*(*(a+i)+j)`。

**拓展**

+ 回答「指针、数组与二维数组」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q05 指针、数组与二维数组|指针、数组与二维数组]]。

### Q06 参数传递与引用

**原题**

+ 「C/C++ 函数参数怎样传递？」 ([2016](test/2016-推研面试经验分享_胡雨婷.pptx)) 〔B 级〕
+ 「传值、指针和引用有什么区别？」 ([2016](test/2016-推研面试经验分享_胡雨婷.pptx)) ([2018](test/2018_面试整体情况.docx)) 〔B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C/C++ 函数参数怎样传递？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。
+ 对 「传值、指针和引用有什么区别？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

C 只有值传递，传指针也是把地址值复制给形参；函数可通过该地址修改被指对象。C++ 引用参数是对象的别名，语法更接近直接使用对象且通常不能为空，但接口设计仍需用 `const` 表明只读。大对象可用 `const T&` 避免复制。

**拓展**

+ 回答「参数传递与引用」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q06 参数传递与引用|参数传递与引用]]。

### Q07 函数重载

**原题**

+ 「C++ 函数重载是什么意思？」 ([2013](test/2013-面试经历小结-黄博.pdf)) 〔A 级〕
+ 「编译器根据参数表还是返回值决定调用哪个重载？」 ([2013](test/2013-面试经历小结-黄博.pdf)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C++ 函数重载是什么意思？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「编译器根据参数表还是返回值决定调用哪个重载？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。

**解答**

同一作用域中同名函数可有不同参数类型或参数个数。编译器先形成候选集，再依据实参与形参的转换等级选择唯一最佳匹配。不能仅靠返回类型重载，因为调用处可能不使用返回值，无法消除歧义。

**拓展**

+ 回答「函数重载」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q07 函数重载|函数重载]]。

### Q08 构造、析构与对象生命周期

**原题**

+ 「构造函数和析构函数什么时候调用？」 ([2012](test/2012-面经_九字班2.pdf)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「构造函数和析构函数什么时候调用？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。

**解答**

构造函数在对象生命周期开始时建立类不变量，析构函数在生命周期结束时释放资源。自动对象按作用域退出逆序析构，成员按声明逆序析构，基类最后析构；动态对象需与对应的 `delete` 配对。RAII 把资源所有权绑定到对象生命周期，可在异常路径上自动清理。

**拓展**

+ 回答「构造、析构与对象生命周期」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q08 构造、析构与对象生命周期|构造、析构与对象生命周期]]。

### Q09 封装、继承与多态

**原题**

+ 「C++ 的封装、继承、多态分别是什么？」 ([2016](test/2016-推研面试经验分享_胡雨婷.pptx)) 〔A/B 级〕
+ 「`public`、`protected`、`private` 有什么区别？」 ([2021](test/2021-八字班推研面经-涂晨昕.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C++ 的封装、继承、多态分别是什么？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「`public`、`protected`、`private` 有什么区别？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

封装把数据与操作绑定并限制外部访问；继承表达基于已有类的扩展和「是一个」关系；运行时多态通过基类接口调用派生类实现。`public` 对外可见，`protected` 对类及派生类可见，`private` 只对类本身和友元可见。应优先以接口和组合表达设计，不为复用代码而滥用继承。

**拓展**

+ 回答「封装、继承与多态」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q09 封装、继承与多态|封装、继承与多态]]。

### Q10 虚函数、纯虚函数与抽象类

**原题**

+ 「虚函数与函数重载有什么区别？」 ([2009](test/2009-推研资料.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「虚函数与函数重载有什么区别？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

重载是在编译期按参数列表选择同名函数；虚函数覆盖是在运行期按对象实际类型分派。纯虚函数通常写为 `virtual f() = 0`，含纯虚函数的类是抽象类，不能直接实例化。若通过基类指针删除派生对象，基类析构函数必须为虚函数。

**拓展**

+ 回答「虚函数、纯虚函数与抽象类」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q10 虚函数、纯虚函数与抽象类|虚函数、纯虚函数与抽象类]]。

### Q11 Python 与 C++

**原题**

+ 「Python 与 C++ 有什么不同？」 ([2021](test/2021-八字班推研面经-涂晨昕.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「Python 与 C++ 有什么不同？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。

**解答**

Python 以动态类型和自动内存管理为主，常由字节码解释器执行，开发快且生态丰富；C++ 静态类型、通常提前编译为本地代码，能精细控制对象布局、所有权与性能。两者都支持面向对象和泛型风格，不能简单概括为「一个解释、一个编译」，因为实现可使用 JIT、扩展模块或解释器。

**拓展**

+ 回答「Python 与 C++」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q11 Python 与 C++|Python 与 C++]]。

### Q12 Python 的 `list` 与 `tuple`

**原题**

+ 「Python 的 `list` 和 `tuple` 有什么区别？哪个效率更高？」 ([2021](test/2021-八字班推研面经-涂晨昕.docx)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「Python 的 `list` 和 `tuple` 有什么区别？哪个效率更高？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

`list` 可变，可增删改元素；`tuple` 不可变，若所有元素可哈希则自身可作字典键。CPython 中 tuple 结构更紧凑，创建和迭代常略快，但差异依实现和场景而定，不能把「更高效」当作选型的唯一理由。

**拓展**

+ 回答「Python 的 `list` 与 `tuple`」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q12 Python 的 `list` 与 `tuple`|Python 的 `list` 与 `tuple`]]。

### Q13 文本文件与二进制文件

**原题**

+ 「文本文件和二进制文件有什么区别？」 ([2017](test/2017-面试经验-陈誉博.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「文本文件和二进制文件有什么区别？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

文本格式把数据编码为字符，便于阅读、交换和调试，但存在解析开销与格式冗余；二进制格式直接按协议编码字段，更紧凑高效，但必须明确字节序、尺寸、版本与兼容性。操作系统层面文件都是字节序列，「文本」与「二进制」主要是解释方式和部分平台 I/O 转换模式的区别。

**拓展**

+ 回答「文本文件与二进制文件」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q13 文本文件与二进制文件|文本文件与二进制文件]]。

### Q14 递归、栈与栈溢出

**原题**

+ 「递归为什么可能失败？怎样改成循环？」 ([2017](test/2017-面试经验-陈誉博.docx)) 〔A/B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「递归为什么可能失败？怎样改成循环？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。

**解答**

每次递归调用通常建立栈帧，深度过大可能栈溢出；重复子问题还会造成指数级重复计算。尾递归可在支持尾调用优化的实现中复用栈帧，但 C++ 不保证。一般递归可用显式栈模拟，动态规划则可把有重叠子问题的递归改为记忆化或自底向上循环。

**拓展**

+ 回答「递归、栈与栈溢出」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q14 递归、栈与栈溢出|递归、栈与栈溢出]]。

### Q15 Debug 与 Release

**原题**

+ 「Debug 与 Release 有什么区别？为什么程序可能只在一种模式出错？」 ([2012](test/2012-面经_九字班2.pdf)) 〔B 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「Debug 与 Release 有什么区别？为什么程序可能只在一种模式出错？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

Debug 通常关闭或降低优化、保留符号和断言，便于单步调试；Release 开启优化并可能定义 `NDEBUG`。只在某模式失败常暴露未初始化变量、越界、数据竞争、悬空指针或对求值顺序的错误依赖，本质是程序含未定义行为，而非「优化器把正确程序优化错」。

**拓展**

+ 回答「Debug 与 Release」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q15 Debug 与 Release|Debug 与 Release]]。

### Q16 运算符重载

**原题**

+ 「为什么要重载运算符？哪些运算符不能重载？」 ([2022](test/2022-九字班推研面经-1.docx)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「为什么要重载运算符？哪些运算符不能重载？」，需要给出因果链或运行机制，并说明结论赖以成立的条件。

**解答**

运算符重载让用户自定义类型沿用自然的表达式语法，例如复数相加或智能指针解引用，但应保持原运算符的直觉语义。C++ 不能重载 `.`、`.*`、`::`、`?:`、`sizeof` 等运算符，也不能改变运算符的优先级、结合性和操作数个数。至少一个操作数必须是用户定义类型。

**拓展**

+ 回答「运算符重载」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q16 运算符重载|运算符重载]]。

### Q17 抽象类与接口

**原题**

+ 「什么是抽象类？」 ([2023](test/2023-0字班推研面经-李响.pdf)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「什么是抽象类？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。

**解答**

含有至少一个纯虚函数的 C++ 类是抽象类，不能直接实例化，但可定义公共接口并由派生类实现。抽象类仍可有数据成员、普通成员函数、构造函数和纯虚函数的定义。作为多态基类使用时，析构函数应为虚函数。

**拓展**

+ 回答「抽象类与接口」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q17 抽象类与接口|抽象类与接口]]。

### Q18 指针类型与友元函数

**原题**

+ 「C 语言中有哪些常见指针？」 ([2024](test/2024-一字班推研面经-zcy.docx)) 〔A 级〕
+ 「友元函数能否访问类的私有成员？」 ([2024](test/2024-一字班推研面经-吴凌沨.pdf)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 2 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「C 语言中有哪些常见指针？」，需要先给出准确概念，再说明核心关系、成立条件和典型用途。
+ 对 「友元函数能否访问类的私有成员？」，需要先给出带条件的结论，再用反例、极限情形或失败场景说明边界。

**解答**

可按所指对象区分对象指针、函数指针、指向指针的指针、数组指针和 `void*`；「指针数组」是元素为指针的数组，不是另一种指针语义。C++ 友元函数不是成员函数，但经类内 `friend` 声明后可访问该类的 `private` 和 `protected` 成员；友元会削弱封装，应只用于确有对称运算或紧耦合协作的场景。

**拓展**

+ 回答「指针类型与友元函数」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q18 指针类型与友元函数|指针类型与友元函数]]。

### Q19 32 位与 64 位计算机

**原题**

+ 「32 位和 64 位计算机有什么区别？为什么现在更多使用 64 位？」 ([2024](test/2024-一字班推研面经_高希钢.docx)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「32 位和 64 位计算机有什么区别？为什么现在更多使用 64 位？」，需要在定义、机制、代价和适用场景上逐维比较，不能只罗列名词。

**解答**

位数通常指通用寄存器和虚拟地址等体系结构宽度，不表示所有指令都恰为该长度。64 位架构可提供更大的地址空间、更多或更宽寄存器及相应算术能力，但指针变大会增加内存占用和缓存压力。实际可寻址范围还受处理器实现和操作系统限制。

**拓展**

+ 回答「32 位与 64 位计算机」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q19 32 位与 64 位计算机|32 位与 64 位计算机]]。

### Q20 中文字符编码

**原题**

+ 「常见中文编码有哪些？怎样判断一段字节按某编码是否合法？」 ([2024](test/2024-一字班推研面经-李羿璇.pdf)) 〔A 级〕

**参考解答**

**分析**

这组问题包含 1 条原题或连续追问。作答时应按提问顺序覆盖，不能只解释最后出现的术语。

+ 对 「常见中文编码有哪些？怎样判断一段字节按某编码是否合法？」，需要给出可复现的步骤、推导或结构，并检查边界条件与实现约束。

**解答**

常见编码包括 GB2312、GBK、GB18030 和 Unicode 的 UTF-8、UTF-16 等编码形式。不能仅看某一字节就普遍判断「是不是中文」；需按选定编码的前导位、后续字节范围和码点映射验证完整字节序列。中国国家强制标准相关场景应关注 GB18030，不能把 GB2312笼统称为现行唯一官方中文编码。

**拓展**

+ 回答「中文字符编码」时应区分语言标准、编译器实现和操作系统行为，并指出未定义行为、实现定义行为或资源所有权。
+ 可用一个最小代码片段解释对象生命周期、类型转换或内存布局，再补充时间、空间、安全性和可维护性代价。

**关联知识点**

+ [[计算/CP - 计算机程序设计基础/QUESTIONS#Q20 中文字符编码|中文字符编码]]。
